`git -- version`

`cd` me sirve para navegar entre carpetas 

`cd ..` para devolver una carpeta

`git init`  para inicializar repositorio vacio
git status  para verificar el estado de archivos 
Set-
git add . agregar los archivos a mi got (caja)
git commit -m "first commit" para sellar la caja 
git clone https://github.com/fernandogallego75/BootCampProfesG706.git


python --version
crear un entorno virtual en python
`python -m venv env312`
activarlo ``env312\scripts\activate``
``deactivate`` para desactivar
``pip list`` permite verificar las librerias